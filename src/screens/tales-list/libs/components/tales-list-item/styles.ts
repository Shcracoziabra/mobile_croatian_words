import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	container: {
		maxWidth: 380,
		minWidth: 300,
	},
	absoluteCover: {
		height: 150,
		width: '100%',
	},
});

export { styles };
