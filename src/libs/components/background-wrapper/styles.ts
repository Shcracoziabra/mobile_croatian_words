import { StyleSheet } from '~/libs/components/components';

const styles = StyleSheet.create({
	absoluteCover: {
		left: 0,
		position: 'absolute',
		top: 0,
		height: '100%',
		width: '100%',
	},
});

export { styles };
