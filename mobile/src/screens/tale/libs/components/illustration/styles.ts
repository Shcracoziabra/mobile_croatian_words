import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	container: {
		height: 300,
		maxHeight: 300,
		width: '100%',
	},
	image: {
		height: '100%',
		width: '100%',
	},
});

export { styles };
